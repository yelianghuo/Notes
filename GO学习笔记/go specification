go 说明书
=======



**`...`parameters**  
`...T`在函数参数中是可变参数，它等价于数组切片`[]T`。如果在函数调用时，没有实参传给可变形参，则可变形参会被传入nil，否则就会
将传入的多个参数当做**新的数组切片**传递给可变形参。如果实参是切片类型`[]T`，并且在将实参p后面跟上`...`,那我们就可以实现
引用传递切片，没有新的切片会被创建。
 
